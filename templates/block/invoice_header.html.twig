<!-- Header Section -->
<header>
    <nav class="navbar navbar-expand-lg navbar-dark " style="background-color: #F70081;opacity: 0.7;">
        <div class="container">
            <a class="navbar-brand" href="#" style="width: 200px;">
                <img src="{{asset('asset/img/ccgroup.png')}}"></img>
            </a>
            <button class="navbar-toggler bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#mob-navbar"
                aria-label="Toggle">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="mob-navbar">
                <ul class="navbar-nav mb-2 mb-lg-0 ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('reglage')}}"><i
                                class="fas fa-tools mx-2"></i>Reglages</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('invoice')}}"><i
                                class="fas fa-file-invoice mx-2"></i>Factures</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('produit')}}"><i
                                class="fas fa-user-friends mx-2"></i>Mes Produits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('mes_client')}}"><i
                                class="fas fa-user-friends mx-2"></i>Mes Clients</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('index')}}"><i
                                class="fas fa-sync mx-2"></i>Historiques</a>
                    </li>
                    {% if app.user %}
                    <li class="nav-item ">
                        <a class="nav-link active" href="{{path('app_logout')}}"><i
                                class="fas fa-power-off mx-2"></i>Se
                            Deconnecter</a>
                    </li>
                    {% elseif not app.user %}
                    <li class="nav-item ">
                        <a class="nav-link active" href="{{path('app_login')}}"><i class="fas fa-power-off mx-2"></i>Se
                            Connecter</a>
                    </li>
                    {% endif %}
                </ul>
                <!-- <form class="d-flex">
                    <a href="{{path('app_login')}}" class="btn btn-outline-warning mx-2 ">Connexion</a>
                    <a href="{{path('app_register')}}" class="btn btn-outline-info" type="submit">Créer un
                        compte</a>
                </form> -->
            </div>
        </div>
    </nav>
</header>